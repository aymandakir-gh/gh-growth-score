# ─────────────────────────────────────────────────────────────
# gh-growth-score — Next.js env vars
# Copy to .env.local and fill in. NEVER commit .env.local.
# ─────────────────────────────────────────────────────────────

# gh-leads-core backend URL (server-only — never exposed to browser)
# Leave empty to run in dev mode without lead storage.
LEADS_API_URL=https://your-gh-leads-core-host.railway.app
